export class Coach {
  constructor(
    public name: string,
    // public picture: string,
    public email: string,
    public level: string,
    public title: string,
    public bio: string
  ) {}
}